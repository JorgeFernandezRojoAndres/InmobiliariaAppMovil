<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- ✅ Permitir HTTP en LAN -->
    <domain-config cleartextTrafficPermitted="true">
        <!-- Emulador local -->
        <domain includeSubdomains="true">10.0.2.2</domain>

        <!-- IPs LAN válidas -->
        <domain includeSubdomains="true">192.168.1.33</domain>

        <domain includeSubdomains="true">192.168.1.34</domain>
        <domain includeSubdomains="true">192.168.1.36</domain>

        <!-- Localhost para pruebas en PC -->
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>

    <!-- 🔒 Bloquear tráfico HTTP fuera de la red local -->
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
